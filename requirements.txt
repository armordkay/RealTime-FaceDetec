fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
minio
websockets
PyJWT
python-dotenv
requests
numpy
Pillow
deepface
tf-keras
# torch (CPU-only) bắt buộc cho module chống ảnh giả (anti-spoofing) của DeepFace.
# Máy/Docker không GPU dùng đúng wheel CPU qua index dưới đây.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.12.0+cpu
